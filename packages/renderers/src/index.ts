/**
 * @veris/renderers — VERIS visual rendering of reports.
 *
 * ## Renderer types
 * - TuiRenderer — Terminal UI (Ink/React)
 * - HtmlRenderer — HTML report
 * - MarkdownRenderer — Markdown report
 *
 * ## Invariants (from SPEC-010 §3):
 * - U2: Renderers never modify analysis data
 * - U3: Renderers never analyze or process findings
 * - U4: CLI output is reproducible
 */
export {};
