import { describe, it, expect } from 'vitest';

describe('@veris/core', () => {
  it('should export types correctly', () => {
    // Verify the module can be imported without errors
    // Actual type tests will be added in Phase 2+
    expect(true).toBe(true);
  });
});
