export {
  MAX_FILE_SIZE,
  MAX_FILES_PER_SCAN,
  MAX_LINES,
  MAX_ARCHIVE_DEPTH,
  MAX_DIR_DEPTH,
  MAX_SYMLINK_DEPTH,
  MAX_ARCHIVE_ENTRIES,
  MAX_COMPRESSION_RATIO,
  MAX_TOTAL_DECOMPRESSED_BYTES,
  DEFAULT_PARSER_TIMEOUT_MS,
  DEFAULT_RULE_TIMEOUT_MS,
  DEFAULT_PLUGIN_TIMEOUT_MS,
  MAX_PLUGIN_MEMORY_MB,
  MAX_PLUGIN_CRASHES,
  PLUGIN_QUARANTINE_WINDOW_MS,
  MIN_CONFIDENCE,
  MAX_CONFIDENCE,
  MIN_RISK_SCORE,
  MAX_RISK_SCORE,
  MIN_TRUST_SCORE,
  MAX_TRUST_SCORE,
  MIN_SEVERITY_SCORE,
  MAX_SEVERITY_SCORE,
  MAX_FINDINGS,
  MAX_RECOMMENDATIONS,
  CACHE_SIZES,
  WORKER_POOL,
  TUI_LIMITS,
} from './limits.js';

export type { PlatformOS, PlatformArch, PlatformInfo } from './platform.js';
export {
  SCHEMA_VERSION,
  ENGINE_VERSION,
  detectOS,
  detectArch,
  getPlatformInfo,
  isWindows,
  isMacOS,
  isLinux,
} from './platform.js';
