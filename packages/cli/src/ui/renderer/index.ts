/**
 * @veris/cli/ui/renderer — Symbol and output rendering system.
 *
 * Provides Unicode with automatic ASCII fallback for all terminal output.
 *
 * @module @veris/cli/ui/renderer
 */

export { getSymbolSet, resetSymbolSet, setSymbolSet, symbol, type SymbolSet } from './symbols.js';
